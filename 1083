#include<stdio.h>
int main()
{ 

	char a[11],b[11];
	while(scanf("%s%s",a,b)!=EOF)
	{
		int i,j,ans=0;
		for(i=0;a[i]!='\0';i++)
		
		  for(j=0;b[j]!='\0';j++)/*从上一行复制的 a忘了改成b*/
		
			ans+=(a[i]-'0')*(b[j]-'0');
		
		
		
		
		printf("%d\n",ans);
	}
	return 0;
}
